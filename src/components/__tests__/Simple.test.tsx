import { describe, it, expect } from 'vitest';
import PaperPage from '../PaperPageJS';

describe('Simple Test', () => {
    it('imports PaperPage', () => {
        expect(PaperPage).toBeDefined();
    });
});
